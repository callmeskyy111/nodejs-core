import { addNums, subNums } from "./math.js";

const addition = addNums(10, 5);
const subtraction = subNums(20,8);
console.log("The addition is:", addition);
console.log("The subtraction is:", subtraction);
